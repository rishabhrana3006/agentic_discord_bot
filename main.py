# TODO: Create discord.Client / Bot with intents (message_content=True)
# TODO: on_message — ignore message.author.bot
# TODO: Rule: exact "ayy" -> reply "hello"
# TODO: Rule: ~query "..." -> call LLM via httpx + LLM_ENDPOINT / LLM_TOKEN
# TODO: Rule: "x or y" -> LLM pick with system prompt
# TODO: main() — load config from config.py, client.run(settings.discord_bot_token)


def main():
    pass


if __name__ == "__main__":
    main()
